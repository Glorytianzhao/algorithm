1 继承
2 装饰器
  强调对原有类的增强,将被装饰类直接注入进来
3 代理模式
  强调对访问的控制 构造方式直接new一个该类对象
  静态代理
  动态代理
   CGlib 动态代理 可以直接代理类
   JDK 本身的动态代理 需要被代理类集成某个接口

静态代理和动态代理的区别
静态：由程序员创建代理类或特定工具自动生成源代码再对其编译，在程序运行前代理类的 .class 文件就已经存在了。
动态：在程序运行时，运用反射机制动态创建而成